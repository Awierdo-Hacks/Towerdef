/**
 * Map representation: the tile grid, tile types, and enemy paths.
 *
 * <p>{@code GameMap} loads a level from a {@code .properties} file and builds a grid of
 * {@code Tile}s (each with a {@code TileType}), the ground and optional aerial
 * {@code Path}s enemies follow, the spawn point, the base position, and the buildable
 * tower spots.</p>
 *
 * @author Tower Defence team
 */
package be.uantwerpen.fti.ei.geavanceerde.towerdefence.game.map;
