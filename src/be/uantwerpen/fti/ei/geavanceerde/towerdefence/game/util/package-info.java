/**
 * Shared utility classes for the game logic.
 *
 * <p>Contains {@code Position} (double-precision game-world coordinates),
 * {@code Stopwatch} (frame-independent delta-time measurement), and
 * {@code ConfigManager} (loading game parameters from {@code .properties} files with
 * safe defaults).</p>
 *
 * @author Tower Defence team
 */
package be.uantwerpen.fti.ei.geavanceerde.towerdefence.game.util;
