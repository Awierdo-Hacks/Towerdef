/**
 * Concrete Java2D visual entities.
 *
 * <p>Each class here extends an abstract game-logic entity (e.g. {@code J2dArrowTower}
 * extends {@code ArrowTower}) and implements {@code render()} with {@code Graphics2D},
 * drawing a sprite (with a coloured-shape fallback). They add presentation only — all
 * gameplay behaviour stays in the abstract superclasses in
 * {@code game.entities}/{@code game.towers}/{@code game.enemies}.</p>
 *
 * @author Tower Defence team
 */
package be.uantwerpen.fti.ei.geavanceerde.towerdefence.j2d.entities;
